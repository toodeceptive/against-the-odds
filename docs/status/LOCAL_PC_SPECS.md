# Local PC specs (compatibility and performance)

**Purpose:** One-time snapshot for agent compatibility and performance notes. No sensitive data. Update when environment changes.

---

## Gathered (2026-01-30)

- **Node:** v24.12.0
- **npm:** 11.6.2
- **Repo path:** `c:\Users\LegiT\against-the-odds` (or worktree path as used)
- **OS / RAM / disk:** Optional — run `systeminfo` (Windows) or fill manually if needed for troubleshooting.

---

## Local-only requirements

- **`.env.local`** at repo root only (never committed). See [docs/CREDENTIALS_SETUP.md](../CREDENTIALS_SETUP.md).
- **Shopify CLI auth:** `shopify auth login` stored in user profile; theme dev preview runs locally.
- No other repo content is required to be stored in a different desktop folder.

---

## Runbook note

For “remaining setup” and env checks, see [OPERATOR_RUNBOOK.md](../../OPERATOR_RUNBOOK.md) and [docs/ENVIRONMENT_SETUP.md](../ENVIRONMENT_SETUP.md).
